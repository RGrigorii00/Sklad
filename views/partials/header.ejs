<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Togglenavigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <div class="header-item">
                            <li class="nav-item">
                                <a class="nav-link text-dark" href="/"><i class="bi-house"></i> Главная</a>
                            </li>
                        <div class="header-item">
                            <% if (sess.username != undefined) { %>
                                <li class="nav-item">
                                    <a class="nav-link disabled" aria-disabled="true"><i class="bi bi-person"></i> Вы вошли как - <%=sess.username%></a> 
                                </li>
                            <% } %>
                            <% if (sess.username == undefined) { %>
                                <li class="nav-item">
                                    <a class="nav-link text-success" href="/login"><i class="bi bi-box-arrow-in-right"></i> Войти</a>
                                </li>
                            <% } else { %>
                                <li class="nav-item">
                                    <a class="nav-link text-danger" href="/logout"><i class="bi bi-box-arrow-right"></i> Выйти</a>
                                </li>
                            <% } %>
                        </div>
                    </div>
                </ul>
            </div>
        </div>
    </nav>
</header>